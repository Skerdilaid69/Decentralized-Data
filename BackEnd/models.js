const db = require('./db');

const Course = {
    getAll: async (search, language, level) => {
        
    },

    getById: async (id) => {
        
    },

    getRecommendations: async (id) => {
        
    }
};

module.exports = Course;